<!DOCTYPE html>
<html>
	{% include 'HeadView.html.twig' %}
<body>

	{% include 'HeaderView.html.twig' %}

	<div class="main_container">
		<div class="screen">
            <a href="{{base_url()}}/{{idBox}}/Pay"><button class="boutton">Paiement classique</button></a>
		    <a href="{{base_url()}}/{{idBox}}/Cagnotte"><button type="submit" class="boutton">Payer via cagnotte</button></a>
        </div>
    </div>
</body>
</html>