<!DOCTYPE html>
<html>
	{% include 'HeadView.html.twig' %}
<body>

	{% include 'HeaderView.html.twig' %}

	<div class="main_container">
        <h2>Cagnotte pour le coffret {{nameBox}}</h2>
            <div class="containBox">
                {% for prestation in tabPrestations %}
                    <div class="itemPay">
                        <div class="grid_item_pres"><img src="{{base_url()}}/web/img/{{prestation[0]}}"/></div>
                        <p>x{{prestation[1]}}</p>
                    </div>
                {% endfor %}
            </div>
        <p>Total : {{sum}}€</p>
        <p>Total déjà payé : {{totalPayeBox}}€</p>
        <form action="" method="post">
            <label>Votre participation</label>
            <input type="number" name="participation" placeholder="10" required/>
            <label>Laissez un message</label>
            <input name="msg" type="texte"/>
            <button type="submit" class="boutton buttonPay">Participer</button>
        </form>
    </div>
</body>
</html>