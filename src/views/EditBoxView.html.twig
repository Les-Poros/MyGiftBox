<!DOCTYPE html>
<html>
{% include 'HeadView.html.twig' %} 

<body>
{% include 'HeaderViewEdit.html.twig' %} 

    <div class="main_container">
         <div class="nomCoffret">
         <h2> {{nomCoffret}} <h2>
         </div>
        <div class="grid_container_prestations">
            {% for values in img %}
            <div class ="detailsPrestations">
			<div class="grid_item_pres"><img src="{{base_url()}}/web/img/{{values[0]}}"/> x{{values[1]}}</div>
            </div>
            {% endfor %}
            
    </div>
    </div>
</body>

</html>